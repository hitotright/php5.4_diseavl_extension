// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("diseval", "for diseval support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("diseval", "enable diseval support", "no");

if (PHP_DISEVAL != "no") {
	EXTENSION("diseval", "diseval.c");
}

